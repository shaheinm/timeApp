<!DOCTYPE html>
<html lang="en">
<head ng-app>
    <meta charset="utf-8" />
    <title>Enter your time and make Dawn happy!</title>
    <style type="text/css">
        body {
            text-align: center;
        }

       /* .modal {
            position: fixed;
            top: 30%;
            left: 35%;
            width: 350px;
            height: 200px;
            background-color: #fff;
            text-align: center;
            padding: 15px;
            text-align: center;
            border: 2px solid #333;
            z-index: 10;
            box-shadow: 0 0 0 9999px rgba(0,0,0,0.5);
        }

            .modal h2 {
                margin: 0px;
                padding: 10px 0 10px 45px;
                border-bottom: 1px solid #333;
                font-size: 20px;
            }*/
    </style>

</head>
<body> <!--ng-controller="MainCtrl as ctrl"-->

        <h1>Hello <span ng-bind="ctrl.name"></span>!</h1>

    <!--div class="modal">
        <form ng-submit="ctrl.submit()" name="timeForm"-->
            <h3> Who are you? </h3>
            <input type="text"
                   ng-model="ctrl.name"
                   required
                   placeholder="Enter your name" />
            <!--br />
            <input type="submit"
                   value="Punch In"
                   ng-disabled="timeForm.$invalid" /-->
        <!--/form-->
    <!--/div-->

        <h3>Date: </h3><input type="date"

                              ng-model="date"
                              placeholder="What day is it?" />

        <h3>Project: </h3><input type="text"
                                 ng-model="project"
                                 placeholder="Who are you working for?" />

        <h3>Hours: </h3><input type="text"
                               ng-model="hours"
                               placeholder="Tell us how long it took" />

        <h3>Description: </h3><textarea ng-model="description" placeholder="Anything else?"> </textarea>
        <br />
        <br />
        <button data-ng-disabled="progress.active()" data-ng-click="submit()" class="btn btn-default">Submit</button>
        <br />
        <br />
        <img src="~/Content/nancy-logo.png" alt="Nancy logo" /><br />
        This view was rendered using the Nancy Razor view engine
<footer>
    <p>This app was developed in all of its simplisitic glory by Shahein Moussavi</p>
</footer>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.0/angular.min.js"></script>
    <!--script type="text/javascript">
        angular.module('timeApp', [])
            .controller('MainCtrl', [function () {
                var self = this;
                self.submit = function () {
                    console.log('User clicked submit with ',
                        self.name);
                };
            }]);
    </script-->
</body>
</html>
